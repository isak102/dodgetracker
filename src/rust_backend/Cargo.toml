[package]
name = "rust_backend"
version = "0.1.0"
edition = "2021"

[dependencies]
dotenv = "0.15.0"
futures = "0.3.30"
sea-orm = { version = "^0.12.0", features = [
  "sqlx-mysql",
  "runtime-async-std-native-tls",
  "macros",
] }
